<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Customer Data</title>
</head>
<body>
    <nav class="navbar navbar-light navbar-expand-lg fixed-top shadow-sm bg-white"><a class="navbar-brand"></a>
        <button type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler"><span class="navbar-toggler-icon"></span></button>
        <div id="navbarSupportedContent" class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a href="/customers" class="nav-link">ALL DATA</a></li>
                <li class="nav-item"><a href="/customers/new" class="nav-link">ADD WAIVER</a></li>
            </ul>
        </div>
    </nav>
    <h1 class="h1Index">Customer Details</h1>
    <section class="marginTop">
        <div class="row">
            <dt class="col-sm-3">Customer First Name:</dt>
            <dd class="col-sm-9"><%= customer.first %></dd>
        </div>
        <div class="row">
            <dt class="col-sm-3">Customer Last Name:</dt>
            <dd class="col-sm-9"><%= customer.last %></dd>
        </div>
        <div class="row">
            <dt class="col-sm-3">Phone number:</dt>
            <dd class="col-sm-9"><%= customer.phone %></dd>
        </div>
        <div class="row">
            <dt class="col-sm-3">Email ID:</dt>
            <dd class="col-sm-9"><%= customer.email %></dd>
        </div>
    </section>
    <section class="marginTop">
        <form method="POST"
    action="/customers/<%= customer._id %>/note">
            <div class="col-6 form-group">
                <label class="form-label">Note</label>
                <textarea class="form-control" name="notes" rows="4" cols="50" placeholder="Add note"></textarea>
            </div>
            <div class="row">
                <div class="col-6 marginTop">
                    <button type="submit" class="btn btn-primary">ADD NOTE</button>
                </div>
            </div>
        </form>
    </section>
    <section class="marginTop">
        <table class="table">
            <thead class="thead-dark">
                <th>Notes</th>
                <th></th>
            </thead>
            <tbody>
                <% customer.notes.forEach(function(r) { %>
                    <tr>
                        <td><%= r.notes %></td>
                        <td>
                            <form action="/notes/<%= r._id %>?_method=DELETE" method="POST">
                               <button class="btn btn-outline-danger btn-sm" type="submit">DELETE</button>
                            </form>
                          </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </section>
</body>